<div>


    <h3>{{challengeId.name}}</h3>
    <img src='{{challengeId.image}}' alt=" Challenge Image">
    <p>{{challengeId.description}}</p>

</div>

<form action="/challenge/{{challengeId._id}}/accept" method="post">

    <button type="submit">Take it!</button>

</form>